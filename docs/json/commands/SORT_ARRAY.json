{
  "name": "SORT ARRAY",
  "abbreviation": null,
  "platform": null,
  "description": "ASC/DSC - Optional - Which way to sort. Use ASC for ascending (names and numbers get larger as the array element number increases) or DSC for descending (names and numbers get smaller as array element number increases).\nThis is the TAS Professional 3.0 command here for compatibility. The preferred TAS Professional 5.1 command is SORT ARRAY.",
  "syntax": "SORT ARRAY",
  "parameters": [
    {
      "name": "mem_area#",
      "type": "f/c/e",
      "required": true,
      "description": "This is the memory area to use. This must resolve to a value of 1 through 4."
    },
    {
      "name": "sort_expression",
      "type": "f/c/e",
      "required": true,
      "description": "This resolves to the value to sort."
    },
    {
      "name": "size_of_sort_expr",
      "type": "f/c/e",
      "required": true,
      "description": "The internal size of the value to sort."
    },
    {
      "name": "num_flds_to_sort",
      "type": "f/c/e",
      "required": true,
      "description": "This is the maximum number of entries that will be made to the sort buffer. This is only required when initializing the sort buffer at the beginning of the process."
    },
    {
      "name": "chg_value",
      "type": "f/c/e",
      "required": true,
      "description": "The amount to add to or subtract from each pointer. NOTE: The size and type of this field must be the same as the pointer."
    },
    {
      "name": "what_to_do",
      "type": "f/c/e",
      "required": true,
      "description": "This must resolve to either 0 (initialize the sort buffer) or 1 (insert a new item into the buffer)."
    }
  ],
  "comments": "This is the equivalent of the TAS Professional 3.0 command Sort.",
  "example": null,
  "sample_program": "SORTEST",
  "program_editor": "Field -> Array -> Sort",
  "see_also": [
    "RDA",
    "WRTA"
  ],
  "is_alias": false,
  "alias_target": null
}