{
  "name": "SAY",
  "abbreviation": null,
  "platform": null,
  "description": "This command will add a field/expression to the screen buffer. This would have the same effect as though it were included in the original MOUNT command.",
  "syntax": "SAY fieldname AT starting_column,row COLOR color PICT picture",
  "parameters": [
    {
      "name": "fieldname",
      "type": "fn/v/e",
      "required": true,
      "description": "The field/expression to be added to the screen buffer."
    },
    {
      "name": "starting_column",
      "type": "f/c/e",
      "required": false,
      "description": "The number of the column at which to start the display of the SAY field. The first column on the screen (far left position) is number 1. The default value, if none is provided, is the current column value as set by the last display or enter to the screen. This value can be easily determined through the use of the COL() function."
    },
    {
      "name": "row",
      "type": "f/c/e",
      "required": false,
      "description": "The row or line number at which to display the SAY field. The first row on the screen (top) is number 1. The default value, if none is provided, is the current row value as set by the last display or enter to the screen. This value can be easily determined through the use of the ROW() function."
    },
    {
      "name": "color",
      "type": "f/c/e",
      "required": false,
      "description": "This is the color value to be used when displaying the field or expression in fieldname."
    },
    {
      "name": "picture",
      "type": "f/c/e",
      "required": false,
      "description": "This is a picture value to be used when displaying or entering the field."
    }
  ],
  "comments": "You should MOUNT some sort of a screen before using this command, even if the screen is blank. You\ncan create a blank screen by running the Screen Painter and then, when the initial screen is displayed,\npresing ESC and saving it at that time. If you have the Source Code to the Utilities a screen format\nnamed CLR_SCR.SCP was included with your programs. This is a blank screen.",
  "example": null,
  "sample_program": "SAYTEST",
  "program_editor": "User interface -> Screen control -> saY",
  "see_also": [
    "MOUNT",
    "CLRSF"
  ],
  "is_alias": false,
  "alias_target": null
}